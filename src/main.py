import astroid


class build()

def class_compile(node: astroid.ClassDef, build):
    mro = None
    if len(node.bases) > 0:
        if build.classes.hasbinding
        for base in node.bases:
            if not build.history.hasbinding()



